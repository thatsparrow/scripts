chmod +x "newscript.sh" // chmod changes mode of file to read, write, etc.

clean.sh // cleans cache and old packages
eyes.sh // turns on xflux and sets gps tag to that of local machine 
system_info.sh // self-explanatory 
update.sh // updates package list and installs available updates 
recent_install.sh // shows list of recent installations on local machine
ipscan.sh // shows IP and MAC list of all WIFI connected devices
